{
 "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html",
    "clean": "rm -rf dist .parcel-cache"
  },
    "devDependencies": {
    "parcel": "2.16.3"
  },
  "targets": {
    "default": {
      "distDir": "dist/assets",
      "publicUrl": "/assets/"
    }
  }
}
